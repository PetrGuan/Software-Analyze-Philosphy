---
description: "Analyze critical logic using the Hoare mindset: define and verify pre/postconditions and invariants."
argument-hint: "[function, module, or critical path to analyze]"
---

# Hoare Analysis

Canonical mindset spec: `architecture/hoare.md`.

Read that file first, then apply it to `$ARGUMENTS`.
